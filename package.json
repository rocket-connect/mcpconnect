{
  "name": "mcpconnect",
  "version": "0.0.21",
  "description": "Build and debug Model Context Protocol integrations with a browser-based interface",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "debugging",
    "development",
    "cli",
    "react",
    "components"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/rocket-connect/mcpconnect.git"
  },
  "bugs": {
    "url": "https://github.com/rocket-connect/mcpconnect/issues"
  },
  "homepage": "https://github.com/rocket-connect/mcpconnect#readme",
  "license": "MIT",
  "author": {
    "name": "rconnect.tech",
    "url": "https://rconnect.tech"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm build:schemas && pnpm build:base-adapters && pnpm build:adapters && pnpm build:components && pnpm build:server && pnpm build:ui && pnpm build:cli",
    "build:schemas": "pnpm --filter @mcpconnect/schemas build",
    "build:base-adapters": "pnpm --filter @mcpconnect/base-adapters build",
    "build:adapters": "pnpm --filter '@mcpconnect/adapter-*' build",
    "build:adapter-localstorage": "pnpm --filter @mcpconnect/adapter-localstorage build",
    "build:adapter-ai-sdk": "pnpm --filter @mcpconnect/adapter-ai-sdk build",
    "build:cli": "pnpm --filter @mcpconnect/cli build",
    "build:components": "pnpm --filter @mcpconnect/components build",
    "build:ui": "pnpm --filter @mcpconnect/ui build",
    "build:server": "pnpm --filter @mcpconnect/server build",
    "dev": "pnpm -r --parallel dev",
    "dev:schemas": "pnpm --filter @mcpconnect/schemas dev",
    "dev:base-adapters": "pnpm --filter @mcpconnect/base-adapters dev",
    "dev:adapter-localstorage": "pnpm --filter @mcpconnect/adapter-localstorage dev",
    "dev:adapter-ai-sdk": "pnpm --filter @mcpconnect/adapter-ai-sdk dev",
    "dev:ui": "pnpm --filter @mcpconnect/ui dev",
    "dev:server": "pnpm --filter @mcpconnect/server dev",
    "dev:cli": "pnpm --filter @mcpconnect/cli dev",
    "start": "pnpm --filter @mcpconnect/cli tsx src/cli.ts",
    "preview:ui": "pnpm --filter @mcpconnect/ui preview",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "version:patch": "node scripts/version-bump.js patch",
    "version:minor": "node scripts/version-bump.js minor",
    "version:major": "node scripts/version-bump.js major",
    "version:set": "node scripts/version-bump.js",
    "version:bump": "node scripts/version-bump.js",
    "prepare-publish": "node scripts/version-bump.js --prepare-publish",
    "restore-workspace-deps": "node scripts/restore-workspace-deps.js",
    "publish:packages": "pnpm -r --filter '@mcpconnect/schemas' --filter '@mcpconnect/base-adapters' --filter '@mcpconnect/adapter-*' --filter '@mcpconnect/cli' --filter '@mcpconnect/components' --filter '@mcpconnect/server' --filter '@mcpconnect/ui' publish --access public --no-git-checks",
    "test": "echo 'Tests will be added in future versions'",
    "validate": "pnpm type-check && pnpm lint && pnpm build"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.56.0",
    "eslint-plugin-jsx-a11y": "^6.8.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^4.6.2",
    "prettier": "^3.1.1",
    "turbo": "^1.11.2",
    "typescript": "^5.3.3",
    "semver": "^7.5.4"
  },
  "packageManager": "pnpm@8.15.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
